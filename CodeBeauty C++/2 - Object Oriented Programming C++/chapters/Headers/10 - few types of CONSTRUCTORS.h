#pragma once

#include <iostream>
#include<list>

using std::cout;
using std::cin;
using std::endl;

using std::ostream;

class User;
void user_info(User);
ostream& operator<<(ostream&, User&);

void N10_few_types_of_CONSTRUCTORS();